# Render Blueprint — deploys Doodler Guys as a free web service.
# Render → New → Blueprint → connect this repo → Apply. That's it.
services:
  - type: web
    name: doodler-guys
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "22"
